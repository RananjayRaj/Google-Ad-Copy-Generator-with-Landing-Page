```env
# Example environment variables (do NOT commit real values)

# OpenAI (if you use API key based credential creation)
OPENAI_API_KEY=REPLACE_YOUR_OPENAI_API_KEY_HERE

# Google OAuth (only if creating custom Google OAuth app)
GOOGLE_CLIENT_ID=REPLACE_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=REPLACE_GOOGLE_CLIENT_SECRET
GOOGLE_REDIRECT_URI=REPLACE_GOOGLE_REDIRECT_URI

# Google Sheets
GOOGLE_SHEETS_SPREADSHEET_ID=REPLACE_GOOGLE_SHEET_ID

# Firecrawl / scraping service (if you use one)
FIRECRAWL_API_KEY=REPLACE_FIRECRAWL_API_KEY

# Optional: placeholder mapping if you prefer to edit JSON before importing
REPLACE_OPENAI_CREDENTIAL_ID=REPLACE_OPENAI_CREDENTIAL_ID
REPLACE_OPENAI_CREDENTIAL_NAME=REPLACE_OPENAI_CREDENTIAL_NAME
REPLACE_GOOGLE_SHEETS_CREDENTIAL_ID=REPLACE_GOOGLE_SHEETS_CREDENTIAL_ID
REPLACE_GOOGLE_SHEETS_CREDENTIAL_NAME=REPLACE_GOOGLE_SHEETS_CREDENTIAL_NAME
REPLACE_FIRECRAWL_CREDENTIAL_ID=REPLACE_FIRECRAWL_CREDENTIAL_ID
REPLACE_FIRECRAWL_CREDENTIAL_NAME=REPLACE_FIRECRAWL_CREDENTIAL_NAME
